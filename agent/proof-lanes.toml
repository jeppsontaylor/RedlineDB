[fast]
commands = [
  "rtk cargo fmt --check",
  "./scripts/check_file_sizes.sh",
  "rtk cargo check --workspace --locked",
  "rtk cargo test --workspace --quiet --locked",
]

[hygiene]
commands = [
  "rtk cargo fmt --check",
  "./scripts/check_file_sizes.sh",
]

[clippy]
commands = [
  "rtk cargo clippy --workspace --all-targets --locked -- -D warnings",
]

[medium]
commands = [
  "rtk cargo test --workspace --quiet --locked",
  "rtk cargo run -p redlinedb-cli -- --help",
  "rtk cargo run -p redlinedb-server -- --help",
]

[phase8-smoke]
commands = [
  "rtk cargo test --workspace --quiet --locked",
  "rtk cargo run -p redlinedb-cli -- --help",
  "rtk cargo run -p redlinedb-server -- --help",
]

# `compat` test_dir uses the top-level `crates/bench/compat` directory because
# `compat::run_suite` recurses into subdirs, so both `orm/` and `slt/` cases
# are discovered automatically without widening the harness.
[phase9-smoke]
commands = [
  "rtk cargo test -p redlinedb-bench --quiet --locked",
  "rtk cargo run -p redlinedb-bench -- certify --config crates/bench/bench/smoke.toml --out-dir target/bench/certify-smoke --seed 7 --repetitions 1 --warmup 0",
  "rtk cargo run -p redlinedb-bench -- compat --engine both --test-dir crates/bench/compat --seed 7",
]

[phase9-compat-full]
commands = [
  "rtk cargo run -p redlinedb-bench -- compat --engine both --test-dir crates/bench/compat --seed 7 --out target/bench/compat-full.json",
]

[phase9-certification]
commands = [
  "rtk cargo run -p redlinedb-bench -- certify --config crates/bench/bench/certification.toml --out-dir target/bench/certify-certification --seed 7 --repetitions 5 --warmup 1",
]

[phase9-docker-smoke]
commands = [
  "./scripts/bench/xbabe1_sync.sh",
  "./scripts/bench/xbabe1_run.sh rtk cargo test -p redlinedb-bench --quiet --locked",
]

[phase9-recovery-matrix]
commands = [
  "rtk cargo run -p redlinedb-bench -- recover-matrix --config crates/bench/bench/recovery-matrix.toml --out target/bench/recovery-matrix.json --seed 7",
]

# TODO Lane E: failpoint-matrix subcommand and config TBD
[phase9-failpoint-matrix]
commands = [
  "rtk cargo run -p redlinedb-bench -- failpoint-matrix --config crates/bench/bench/failpoint-matrix.toml --out target/bench/failpoint-matrix.json --seed 7",
]

[phase9-xbabe1-certification]
commands = [
  "./scripts/bench/xbabe1_sync.sh",
  "./scripts/bench/xbabe1_run.sh rtk cargo run -p redlinedb-bench -- certify --config crates/bench/bench/certification.toml --out-dir target/bench/xbabe1/certification --seed 7 --repetitions 5 --warmup 1",
  "./scripts/bench/xbabe1_fetch.sh certification",
]

[security]
commands = [
  "rtk cargo audit",
  "rtk cargo deny check",
  "rtk gitleaks detect --source .",
]

[security-local]
commands = [
  "rtk cargo audit",
  "rtk cargo deny check",
  "rtk gitleaks detect --source .",
]

[release]
commands = [
  "rtk cargo build --workspace --release --locked",
]
